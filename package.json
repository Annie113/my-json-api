{
  "name": "mock-api",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --port ${PORT:-3000} --watch db.json"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
